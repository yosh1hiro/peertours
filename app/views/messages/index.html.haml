%h1 Listing messages

%table
  %thead
    %tr
      %th Sender
      %th Recipient
      %th Message
      %th Read
      %th Message thread
      %th
      %th
      %th

  %tbody
    - @messages.each do |message|
      %tr
        %td= message.sender_id
        %td= message.recipient_id
        %td= message.message
        %td= message.read
        %td= message.message_thread_id
        %td= link_to 'Show', message
        %td= link_to 'Edit', edit_message_path(message)
        %td= link_to 'Destroy', message, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Message', new_message_path
